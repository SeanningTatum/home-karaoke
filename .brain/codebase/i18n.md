# i18n

**Library**: remix-i18next + i18next + react-i18next.
**URL strategy**: `/:lng/` prefix on public marketing/auth routes (`/`, `/login`, `/sign-up`); `/dashboard` and `/admin` are **not** locale-prefixed and rely on client-side i18n only.

## Current locale state (2026-07-16)

Verified from [`app/i18n/i18n.ts`](../../app/i18n/i18n.ts):

```typescript
export const supportedLngs = ["en", "zh"] as const;
export const fallbackLng = "en" as const;
export const defaultNS = "common" as const;
export const namespaces = ["common", "auth", "admin", "dashboard", "home", "validation", "room"] as const;
```

**English** (default) and **Mandarin** (`zh`) are shipped. The "adding a new language" steps below describe how to add a third.

## Key files

| File | Role |
|------|------|
| [`app/i18n/i18n.ts`](../../app/i18n/i18n.ts) | Shared config — `supportedLngs`, `namespaces`, `fallbackLng`, `defaultNS`, `localeCookieName` |
| [`app/i18n/i18n.server.ts`](../../app/i18n/i18n.server.ts) | Server-side `RemixI18Next` instance |
| [`app/i18n/i18n.client.ts`](../../app/i18n/i18n.client.ts) | Client-side i18next init |
| [`app/i18n/i18n.d.ts`](../../app/i18n/i18n.d.ts) | Type augmentation for translation key autocomplete |
| [`app/i18n/index.ts`](../../app/i18n/index.ts) | Barrel export |
| [`app/locales/en/*.json`](../../app/locales/en/) | One JSON file per namespace |
| [`app/components/language-switcher.tsx`](../../app/components/language-switcher.tsx) | Locale switcher UI |
| [`app/lib/date-utils.ts`](../../app/lib/date-utils.ts) | `getDateFnsLocale(i18nLocale)` + `formatDate` — maps `en → enUS`, `zh → zhCN` |

## Usage

```typescript
// Component
import { useTranslation } from "react-i18next";
const { t } = useTranslation("admin");
return <h1>{t("dashboard.total_users")}</h1>;

// Loader / meta
import { i18nServer } from "@/i18n/i18n.server";
const t = await i18nServer.getFixedT(request, "admin");
```

A route opts into a namespace via the `handle` export:

```typescript
export const handle = { i18n: ["admin"] };
```

## Conventions

- **Namespaces** (must match `namespaces` const): `common`, `auth`, `admin`, `dashboard`, `home`, `validation`, `room`
- **Keys**: dot-notation snake_case (`dashboard.total_users`)
- **Interpolation**: `"Hello, {{name}}"` → `t("greeting", { name })`

### `room` namespace

Backs the group-karaoke host TV screen (`/room/:code`) and guest phone flow (`/join/:code`) — feat-007, reskinned by feat-008. Shipped in both `en` and `zh` (`app/locales/{en,zh}/room.json`). Top-level sections: `player` (start/resume/skip-error), `banner` (idle/now-singing), `controls` (play/pause/skip/volume/guest-reorder toggle/end-party confirm dialog), `queue` (count with `_one`/`_other` pluralization, drag/move-up/move-to-top, remove), `join` (nickname step + `tabs` + `search`, incl. quota-exceeded and paste-a-link fallback copy), `position` (queue position summary), `lobby` (roster heading/prompt/empty state), `connection` (connecting/reconnecting), `now_up` (celebration headline), `sounds` (mute/unmute labels — feat-008 quick win), `state` (room closed / not found), `reactions` (feat-010 — `bar_label`, `recap_title`, `recap_total` with `_one`/`_other` pluralization in `en` / `_other` only in `zh` (CLDR), `aria_recap`, `aria_sent`).

Note the `upload` namespace (and the `FileUpload` component it backed) was removed 2026-07-16 by feat-008 Phase 2 — see `.brain/features/file-upload/file-upload.md`.

### Theme labels live in `common.theme`

Theme toggle copy (`toggle_label`, `light`, `dark`, `system`) lives under `common.theme.*` — not a standalone namespace. Use `useTranslation("common")` + `t("theme.toggle_label")` etc.

### `admin` namespace additions

`admin.json` gained three sections beyond `dashboard`/`charts`/`insights`/`users`/`sidebar`: `nav_user` (`theme_menu`, `log_out` — the sidebar user-menu dropdown), `site_header` (`github`), and `users_table` (search/role/status placeholders, column headers, row-action labels, pagination copy, and toast messages for impersonate/ban/unban/role-change) backing the admin users data table.

## Adding a new language

1. Create `app/locales/{lng}/*.json` files mirroring every namespace JSON in `app/locales/en/`.
2. Add the locale code to `supportedLngs` in `app/i18n/i18n.ts`.
3. Add a label in `app/components/language-switcher.tsx` (`languageLabels[lng]`).
4. Add a `date-fns` mapping in `app/lib/date-utils.ts` (`localeMap[lng] = …`).
5. Hard-refresh `/`, click the switcher, verify both directions swap live.

## Live language toggle (no full reload)

[`app/components/language-switcher.tsx`](../../app/components/language-switcher.tsx) submits to [`app/routes/api/set-locale.ts`](../../app/routes/api/set-locale.ts) via `useFetcher().submit()`. The action writes the locale cookie via `localeCookie.serialize(lng)` (base64-JSON encoding owned by `createCookie` — see footnote). After the action returns, React Router automatically revalidates the root loader, which re-detects the locale via `i18nServer.getLocale(request)` and returns the new `loaderData.locale`. The existing `useChangeLanguage(loaderData.locale)` hook in [`app/root.tsx`](../../app/root.tsx) then flips `i18next.language`, react-i18next re-renders the tree, and the page swaps copy with no full reload.

> **Anti-pattern**: do **not** call `i18n.changeLanguage(lng)` directly from the switcher. It races with `useChangeLanguage`'s revert-on-mismatch effect in `root.tsx` and the language flips back. Drive the swap through the action + revalidation instead.

> **Cookie format**: React Router's `createCookie` base64-encodes a JSON-stringified value (e.g. `"zh"` → `InpoIg==`). Manually setting `document.cookie = "i18next=zh"` won't be parsed by `i18nServer.getLocale`. Always go through an action that calls `localeCookie.serialize`.
