# guest-avatars run — 2026-07-17

## Step 1 — Plan reviewed (plans/guest-avatars.html, round 1)

```
5 decisions resolved on recommended options; 2 open questions answered (seed=initials; anon cleanup deferred to future account-upgrade feature); session ended by user
```

## Step 2 — Phase 1 — server data plane (opus builder)

```
typecheck EXIT 0; tests 34 files / 453 passed (+24 from 429 baseline). QueueItem.singerAvatarUrl + RosterEntry.avatarUrl (optionalWith null default), room-state ctx threading, DO SessionAttachment.avatarUrl + x-avatar-url header, avatar.ts helpers, UserRepository.setUserImage + SetUserImageInput. No tagToTRPC change.
```

## Step 3 — Phase 2 — upload + serve routes (sonnet builder)

```
POST /api/avatar (session-gated multipart, ValidationError/BucketValidationError 400s, BucketRepository.upload avatars/userId, setUserImage versioned URL) + GET /api/avatar/:userId (R2 stream, immutable cache, ETag, 404). Registered in routes.ts. typecheck/test/build green. Deviation: BucketBindingError removed from catchTags (layer-construction failure, not in E channel — surfaces as 500 via Exit.match, mirrors effect-trpc.ts:221-231).
```

## Step 4 — Phase 3 — client capture UI (sonnet builder)

```
computeCoverCrop + 5 tests, downscaleAvatar canvas wrapper (documented untested boundary), InitialsAvatar optional src (AvatarImage before fallback), nickname-form photo picker (hidden input no capture attr, object-URL preview + revoke, Add/Change/Remove controls, best-effort POST /api/avatar after session flow — join never blocks), join.avatar i18n en+zh. typecheck green, 35 files / 458 tests.
```

## Step 5 — Phase 4 — display wiring (sonnet builder)

```
src threaded into roster-strip (compact+lobby), queue-rail singer chip, lobby queue summary, now-up overlay (NowUpSinger type + fixtures updated). join/* reuse QueueRail — no extra wiring. typecheck green, 458 tests.
```

## Step 6 — effect-ts-enforcer sweep (sonnet)

```
0 critical / 0 major / 0 minor across git diff 411d1b4..HEAD. No throw/try-catch server-side, no Zod, no new tagged errors (tagToTRPC untouched — verified empty diff), tests cover avatar.ts/crop.ts/setUserImage, no process.env.
```

## Step 7 — feature-verifier browser walk

```
PASS — 12/12 golden-path steps + error path (.txt upload → inline error, join proceeds photo-less). GET /api/avatar/:userId 200 immutable. Photo + initials-fallback side-by-side. 0 app-origin network errors x3 runs; caret-color hydration warning = env artifact. Doc: features/guest-avatars/verifications/2026-07-17.md
```

## Step 8 — verify-done + doc-gap closure

```
All functional gates green (typecheck, 458 unit, 2/2 e2e smoke, build, harness 11/11, verification doc PASS confirmed). Initial verdict DO-NOT-SHIP on 3 doc gaps (repository.md setUserImage, frontend.md avatar/photo-capture pattern, i18n.md join.avatar keys) — closed inline.
```

## Step 9 — Annotation fix pass (opus)

```
3 pin-annotations fixed: nickname preview centered (CardHeader grid→flex) + IconCamera placeholder (was IconMicrophone); lobby roster grid 1fr-stretch → flex flex-wrap justify-start (chip flush-left); compact roster nickname dropped (sr-only + title kept). typecheck + 458 tests green. Shots 03/06/09 recaptured, visually confirmed. Note: seed creds are admin@preview.local / Password123! — library.md admin@test.local is stale.
```
