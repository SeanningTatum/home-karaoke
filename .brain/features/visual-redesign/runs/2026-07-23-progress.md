# visual-redesign run — 2026-07-23

## Step 1 — Phase 1 start — tokens & type

```
bun add @fontsource-variable/fraunces@5.3.0 @fontsource-variable/jetbrains-mono@5.3.0; bun remove @fontsource-variable/bricolage-grotesque — lockfile saved, cf-typegen ok. branch feat/visual-redesign off origin/main. 12 gradient-accent usages across 11 tsx files queued for Phase 2 cleanup.
```

## Step 2 — Phase 1+2 — tokens rewritten + gradient sweep

```
app.css: Velvet Stage palette (dark default wine-black/cream/pink/brass, matinee light), Fraunces+JetBrains Mono replace Bricolage, stagelight utilities added, gradient-accent utilities deleted, TV scale re-tuned for serif (weights 600-640, opsz axes). 13 gradient call sites swept to solid primary/secondary. celebration-burst confetti palette re-stopped. typecheck green; 521/521 unit tests pass.
```

## Step 3 — Phase 3 — TV surfaces styled

```
stagelight bg (lobby full, playing dim), JoinPanel ticket (quiet border, Fraunces marquee code + brass rule, mono corner code), NowUpOverlay + ReactionRecap pink-wall gradients replaced with stagelight + brass avatar ring, singer credit in brass. Verified live: lobby screenshot shows marquee signature working.
```
