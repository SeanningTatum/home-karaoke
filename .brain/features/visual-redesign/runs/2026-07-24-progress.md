# visual-redesign run — 2026-07-24

## Step 1 — Phase 4+5 — phone + desktop surfaces

```
join route stagelight-dim + code-marquee header; nickname gate stagelight + serif heading (cascade); auth aside rebuilt as lobby-poster vignette (QR ticket + Fraunces marquee + avatar row, no new i18n keys); landing root stagelight + serif-weight hero fix; dashboard hero card quieted + serif h1 + stagelight-dim layout. typecheck 0 errors; 521/521 tests.
```
