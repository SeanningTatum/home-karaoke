# party-screen-cinema run — 2026-07-24

## Step 1 — kickoff

```
feat-014 in-progress. Plan reviewed round 1: 4/4 decisions on recommended (always-dark playing surface, single-line now-playing bar, channel-aware title cleaning, rail as one instrument panel); both open questions answered 'explore' -> NEXT UP callout + progress indicator in scope. Measured baseline: now-playing 112px+16 gap, video 76%/79% of viewport, empty queue panel 389px w/ ~250px void, participants 64px for one avatar, rail inset 4px vs 80px page margin. Preconditions verified: next-themes attribute=class + .dark class selector in app.css => dark class on container flips tokens; tv-safe padding-block already symmetric.
```

## Step 2 — phases1-4-implemented

```
song-title helper 18/18 tests (3 bugs caught+fixed during TDD: CJK bracket misread as noise by ASCII-only strip -> unicode \p{L}\p{N}; 'official'/'video' missing from vocabulary so (Official Video) survived; raw-title fallback then failed). Full suite 541 passed (541). typecheck 0 errors after adding getCurrentTime/getDuration to app/types/youtube.d.ts. Measured after: now-playing 112px+16 -> 59px; video 682->716px (76%->80%) at 1512x900, 856->890px (79%->82%) at 1920x1080; empty up-next anchored 14px under its label (was floating ~250px down); title 1 line; theater surface isDark=true under app theme=light; rail 336->380px at 1920. Live check caught 3 visual defects, all fixed: '3:33 LEFT' clipping into the rail (added lg:gap-8), stub label wrapping to 3 lines at tv-eyebrow (dropped to text-xs), roster band label too long in mono caps (added lobby.roster_short). Queue rows also cleaned (TV variant only).
```
