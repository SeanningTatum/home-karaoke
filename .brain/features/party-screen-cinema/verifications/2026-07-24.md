# Verification: Party Screen Cinema

- **Slug**: `party-screen-cinema` (feature memo: `.brain/features/party-screen-cinema/party-screen-cinema.md`)
- **Date**: 2026-07-24
- **Verified by**: feature-verifier (Playwright CLI — headless script via `bun`)
- **Base URL**: http://localhost:5175 _(dev server: already running — not started or stopped by this walk)_
- **Role**: admin (`admin@preview.local`) for the host TV + host phone + error-path contexts; anonymous guest session for the phone-guest context
- **Verdict**: ✅ PASS — golden path (lobby regression check → 3-song queue → start party → cinema playing state → theater-surface light/dark split) and the error path both hold; no jsErrors, no networkErrors.

## Golden path

Steps 01–02 prove no regression to the lobby (structurally untouched by this feature). Steps 03–05 build a real multi-song queue and start playback from a second, same-context host phone page. Steps 06–10 are the feat-014 playing-state assertions, ending with the headline theater-surface behavior.

| # | Step | Expected | Observed | Screenshot | Result |
|---|------|----------|----------|------------|--------|
| 01 | Sign in as admin (`admin@preview.local`) | Lands on `/dashboard` | Landed on `/dashboard` | [`01-dashboard.png`](../screenshots/01-dashboard.png) | ✅ |
| 02 | Click `dashboard-host-room-button` → lobby | "Party lobby" heading, QR hero (Fraunces code over brass rule), roster panel + queue panel in right column, NOT forced dark | Heading "PARTY LOBBY"; QR hero with `A3T-GRM` in Fraunces over a brass underline; roster panel ("Be the first to scan and join") + queue panel ("Queue is empty — scan to add songs") stacked in right column; `room-host-view` class = `"...bg-stagelight"` — no `dark` token, so it follows the app theme as required | [`02-room-lobby.png`](../screenshots/02-room-lobby.png) | ✅ |
| 03 | Guest (fresh context, phone 390×844) joins `/join/:code`, submits nickname | Lands on search tab | Guest "Party Guest" landed on `join-search-tab` | [`03-guest-joined.png`](../screenshots/03-guest-joined.png) | ✅ |
| 04 | Guest adds 3 songs via paste-a-link (`FC3y9llDXuM`, `dQw4w9WgXcQ`, `fJ9rUzIMcZQ`) | Queue rail on guest shows 3 items | `room-queue-item` count = 3 after all three paste-submits | [`04-guest-queue-3-songs.png`](../screenshots/04-guest-queue-3-songs.png) | ✅ |
| 05 | Host opens `/join/:code` in a **second page of the same authenticated context** (phone 390×844), enters nickname, opens Controls tab, clicks `controls-start-party` | Controls tab visible (host-only), Start button enabled, click succeeds | `join-tab-controls` visible; `controls-start-party` enabled=true; clicked | [`04b-host-phone-joined.png`](../screenshots/04b-host-phone-joined.png), [`05-host-phone-controls.png`](../screenshots/05-host-phone-controls.png) | ✅ |
| 06 | TV picks up `playback.currentItem` over WebSocket — now-playing bar assertions | One row (< 90px tall); eyebrow "NOW SINGING"; cleaned title with no `\| Karaoke Version` / `(Official Video)` cruft; artist after middot; singer avatar chip; `room-now-singing-remaining` reads "N:NN left"; raw YouTube title still on the title's `title` attribute | Row height < 90px; eyebrow="NOW SINGING"; cleaned title="Teenage Dirtbag" · artist "Wheatus"; singer chip "Party Guest"; remaining="4:04 LEFT"; `title` attr = raw "Wheatus - Teenage Dirtbag" (distinct from — same source string as, not re-derived from — the cleaned display) | [`06-tv-playing.png`](../screenshots/06-tv-playing.png) | ✅ |
| 07 | Progress line `room-song-progress` (`role="progressbar"`) increases over ~10s | `aria-valuenow` increases between two reads 10s apart | role=`progressbar`; `aria-valuenow` 1 → 5 over the 10s wait | [`07-progress-after-wait.png`](../screenshots/07-progress-after-wait.png) | ✅ |
| 08 | Right rail is ONE bordered panel with 3 zones: guest band, UP NEXT list, QR ticket stub | `room-playing-rail` visible; `room-playing-participants` (guest band, "1 GUEST") visible; `room-join-stub` visible with `room-code-text` | All three zones visible in a single bordered panel; rail code text = `A3T-GRM`, matches lobby code | [`08-rail-zones.png`](../screenshots/08-rail-zones.png) | ✅ |
| 09 | First queue row carries `room-queue-next-up`, later rows don't | Exactly 1 `room-queue-next-up` callout, on the first `room-queue-item` | Callout count = 1, present on first row only ("Never Gonna Give You Up" boxed as NEXT UP; "Bohemian Rhapsody" plain) | [`09-next-up-callout.png`](../screenshots/09-next-up-callout.png) | ✅ |
| 10 | Theater surface: set `localStorage.theme = "light"`, reload the playing TV page. Root `room-host-view` must still carry `dark` and render dark; `/dashboard` in the same session must render light | Room stays dark after forcing app theme light; dashboard renders light | `room-host-view` class after reload = `"...bg-stagelight-dim dark"`, computed `background-color` = `oklch(0.168 0.028 348)` (near-black plum) — stays dark; `/dashboard` `<html>` class = `"light"`, renders the cream Matinee theme | [`10a-theater-surface-forced-dark.png`](../screenshots/10a-theater-surface-forced-dark.png), [`10b-dashboard-theme-light.png`](../screenshots/10b-dashboard-theme-light.png) | ✅ |

## Error path

| # | Step | Expected failure surface | Observed | Screenshot | Result |
|---|------|--------------------------|----------|------------|--------|
| E1 | Logged-in host visits `/room/ZZZ-ZZZ` (nonexistent code) | Friendly `room-unavailable` screen with a working back-to-dashboard button, no crash | "Room not found" heading + "No room matches code ZZZ-ZZZ..." copy rendered; `room-back-to-dashboard` visible and, when clicked, navigated to `/dashboard` | [`E1-room-unavailable.png`](../screenshots/E1-room-unavailable.png) | ✅ |

## Console

- **jsErrors** — none.
- **networkErrors** — none (after filtering third-party YouTube-embed noise — see below).

Third-party filtering note: the script excluded console/network entries matching `doubleclick.net`, `googleads`/`googlesyndication`, `google-analytics`, YouTube's `api/stats`/`ptracking`/`generate_204` telemetry beacons, and `googlevideo.com`/`play.google.com` — all fired (if at all) from inside the embedded YouTube IFrame during playback, not from app code. None of the filtered entries were app-origin; grepped the app source for any of those domains/tokens and found zero references outside third-party ad/analytics noise, confirming they are not app defects.

## Findings for main thread

None. All required `data-testid`s existed (`room-lobby-heading`, `room-lobby-roster-panel`, `room-lobby-queue`, `room-now-singing`, `room-now-singing-remaining`, `room-song-progress`, `room-playing-rail`, `room-playing-participants`, `room-join-stub`, `room-code-text`, `room-queue-next-up`, `room-host-view`, `room-unavailable`, `room-back-to-dashboard`) — no gaps hit during the walk.

Minor non-blocking observation (not a defect): the queue-rail item titles for the 2nd/3rd guest-added songs truncate on the phone-controls screenshot ("Rick Astley - Never Gonna Gi…", "Queen – Bohemian Rhapsod…") — that's the phone `join` Queue/Controls tab list, which is out of scope for this feature (feat-014 only reworked the TV playing state), noted only for completeness.

## Verdict rationale

Every assertion in the brief held with direct evidence: the lobby is pixel-verified unchanged in structure and correctly *not* forced dark (class list has no `dark` token pre-playback); the now-playing bar is a single row under 90px tall with a cleaned title ("Teenage Dirtbag · Wheatus" from raw "Wheatus - Teenage Dirtbag"), a working remaining-time readout, and the raw title preserved on the `title` attribute; the progress bar's `aria-valuenow` measurably increased over a real 10-second wait; the right rail is one bordered panel with all three zones (guest band, UP NEXT, QR stub); the NEXT UP callout appeared exactly once, on the first row only; and the headline theater-surface behavior was captured side by side — forcing the app theme to light and reloading the playing TV page left `room-host-view` still carrying `dark` and rendering near-black, while `/dashboard` in the same authenticated session rendered the light Matinee theme. The error path (`/room/ZZZ-ZZZ`) showed the friendly not-found screen with a functioning back-to-dashboard link. No jsErrors or app-origin networkErrors were observed across any context (host TV, guest phone, host phone, error-path).
