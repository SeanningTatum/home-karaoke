# Verification: Layout Evolution

- **Slug**: `layout-evolution` (feature memo: `.brain/features/layout-evolution/layout-evolution.md`)
- **Date**: 2026-07-24
- **Verified by**: feature-verifier (Playwright CLI — headless script via `bun`, chromium bundled + `--disable-extensions`)
- **Base URL**: http://localhost:5174 _(dev server: already running — instructed not to start/stop)_
- **Role**: admin (`admin@preview.local`) for the TV/host context and the error path; anonymous guest for the phone-join context
- **Verdict**: ✅ PASS — golden path (landing → dashboard → lobby → playing) and the unknown-room error path both hold the intended layout, zero app jsErrors, zero app networkErrors

## Golden path

Derived directly from the task's stated steps (landing marketing site → dashboard host hub → room lobby spacing → playing layout via a second guest + host-phone context). Viewports: 1920×1080 for TV pages, 390×844 for phone contexts.

| # | Step | Expected | Observed | Screenshot | Result |
|---|------|----------|----------|------------|--------|
| 01 | Landing `/` | Sticky nav (How it works / Features / Host a party), two-column hero with product screenshot + join-code card, HOW IT WORKS 3 cards, FEATURES grid (6 cards), CTA band with `KQ7-3FP` marquee + Host a party, footer with GitHub link + Sign in | All sections present and in order: nav links `How it works`/`Features` + `Host a party` button; hero "Your living room is the stage" with join-code card (`AB3-XY9` placeholder) beside a TV-screenshot mock labeled `KQ7-3FP`; 3 "How it works" cards; 6-card features grid; CTA band with `KQ7-3FP` marquee heading + "Host a party"; footer with `GitHub` + `Sign in` links | [`01-landing.png`](../screenshots/01-landing.png) | ✅ |
| 02 | Login → `/dashboard` | Slim top bar (wordmark, account chip, Admin button, lang/theme), stage hero with host-room button, PREVIOUS SESSIONS rail with real session cards, FEATURED PLAYLISTS rail with Coming-soon badge + 4 non-interactive cards | Top bar shows wordmark, `Admin Preview · Admin` chip, `Admin` button, `English` selector + theme toggle; stage hero "Ready to host, Admin Preview?" with `Host a karaoke room` button; PREVIOUS SESSIONS rail rendered 12 real session cards (code, date, song count, `LIVE` badge + `Rejoin` on open rooms) — the two originally-seeded rows (`MAB-2QK`/3 songs, `ZTR-88L`/1 song) are pushed off the left edge of the horizontally-scrolling rail by newer rooms this run itself created (rail sorts newest-first, no scroll performed) but the same real-data behavior is directly confirmed via the visible cards; FEATURED PLAYLISTS rail shows the `COMING SOON` badge + 4 non-interactive cards (2000s Throwbacks, Duet Night, OPM Classics, Crowd Pleasers) | [`02-dashboard.png`](../screenshots/02-dashboard.png) | ✅ |
| 03 | Click `dashboard-host-room-button` → `/room/:code` lobby (1920×1080) | "Party lobby" heading + spacing (roster panel content-sized, not fixed-third; queue panel below), QR hero card with Fraunces code over brass rule | `PARTY LOBBY` heading top-left; left column QR hero card with `ME8-C75` in Fraunces over a brass underline rule; right column: roster panel measured 221px tall (within the 200px floor / 40%-viewport ceiling — content-sized, not a fixed one-third split) showing "Be the first to scan and join", queue panel below (688px tall) showing "Queue is empty — scan to add songs" | [`03-room-lobby.png`](../screenshots/03-room-lobby.png) | ✅ |
| 04a | Guest joins via `/join/:code` on a phone viewport, submits nickname, pastes a YouTube URL into the queue | Guest lands on Queue tab post-add, sees "1 song", the added item, and a "You're #1" toast | Nickname "Verifier Guest" submitted; paste-mode toggled; `https://www.youtube.com/watch?v=dQw4w9WgXcQ` submitted; Queue tab shows 1 song ("Rick Astley - Never Gonna...") credited to Verifier Guest, "Added! You're #1" confirmation banner | [`04a-guest-phone-queued.png`](../screenshots/04a-guest-phone-queued.png) | ✅ |
| 04b | Host opens `/join/:code` in a second page **within the same authenticated context** (phone viewport), enters a nickname, opens the Controls tab, starts the party | Controls tab visible only because `isHost` resolves true (session shared with the TV context); `controls-start-party` enabled once queue is non-empty | "Host Controller" nickname submitted; Controls tab visible (host-gated tab, confirms `isHost` check passed); `controls-start-party` button enabled and clicked | [`04b-host-phone-controls.png`](../screenshots/04b-host-phone-controls.png) | ✅ |
| 04 | Back on the TV page: playing layout | Now-singing banner top-left (title + brass "sung by" credit), video large, right rail with participants row + queue rail + corner QR ticket; **no top bar row above the video** (connection pill moved into the rail) | Now-singing banner shows video title + `sung by Verifier Guest` in the brass/accent color top-left; video fills the main column with no header row above it; right rail shows "1 guest has joined" + avatar chip, `Queue` header with `0 SONGS` badge (song moved from queue to now-playing) and empty-queue copy, corner QR ticket card pinned bottom-right with code `ME8-C75`. Connection pill (`room-reconnecting-pill`) is intentionally absent — it's a "quiet pill" per its own source comment that only renders while `connectionStatus` is `connecting`/`reconnecting`, and the socket was steady (`open`) throughout this walk, so its absence here is correct behavior, not a missing element | [`04-room-playing.png`](../screenshots/04-room-playing.png) | ✅ |

## Error path

Fresh, unauthenticated-then-freshly-authenticated `browser.newContext()` (separate from the golden-path TV context) — logged in again as the host, then navigated to a valid-format but unknown room code.

| # | Step | Expected failure surface | Observed | Screenshot | Result |
|---|------|--------------------------|----------|------------|--------|
| E1 | Visit `/room/ZZZ-ZZZ` as a logged-in host | Friendly not-found screen (`room-unavailable`) with a back-to-dashboard button, no crash | "Room not found" heading, "No room matches code ZZZ-ZZZ. Check the code or start a new room." body copy, `Back to dashboard` button — both `room-unavailable` and `room-back-to-dashboard` testids present and visible | [`E1-room-unavailable.png`](../screenshots/E1-room-unavailable.png) | ✅ |

## Console

Collected by the script's `console` + `pageerror` + `response` listeners across all contexts/pages (TV, guest phone, host phone, error-path page) for the full walk. Per task instructions, entries matching third-party YouTube/doubleclick/ad-embed hosts (`youtube.com`, `ytimg.com`, `doubleclick.net`, `googlesyndication`, `googleads`) were filtered out before classification — the embedded `YoutubePlayer` iframe is expected to emit its own ad-pipeline network chatter that is not app code.

- **jsErrors**: none
- **networkErrors**: none

No hydration warnings were observed on any page (landing, dashboard, lobby, playing, join/search/controls, or the not-found screen), so the environment-artifact carve-out was not needed.

## Findings for main thread

- **PREVIOUS SESSIONS rail ordering**: the two fixture rows called out in the task (`MAB-2QK`/3 songs, `ZTR-88L`/1 song) were not directly visible in the `02-dashboard.png` screenshot because this verification run itself (and, presumably, prior runs/dev sessions against this same local D1) created additional rooms that sort ahead of them (newest-first, no pagination/scroll performed in the script). This is expected behavior for a "real data, newest-first" rail, not a defect — flagging only so a future verifier knows to scroll the rail or query fewer prior runs if they need to see the original seed rows specifically.
- No missing `data-testid`s encountered — every selector needed (`hero-host-cta`, `landing-join-card`, `landing-cta-host`, `footer-github`, `dashboard-account`, `dashboard-host-room-button`, `dashboard-sessions`, `dashboard-playlists`, `room-lobby-heading`, `room-lobby`, `room-lobby-roster-panel`, `room-lobby-queue`, `join-nickname-input`, `join-nickname-submit`, `join-tabs`, `join-search-paste-toggle`, `join-paste-url-input`, `join-paste-url-submit`, `join-tab-controls`, `controls-start-party`, `room-playing-rail`, `room-playing-participants`, `room-playing-join`, `room-unavailable`, `room-back-to-dashboard`) existed and worked as documented.
- None otherwise.

## Verdict rationale

Every golden-path assertion held with visual confirmation via screenshot: the landing page renders the full marketing scroll (sticky nav, two-column hero, 3-card how-it-works, 6-card features grid, CTA band, footer), the dashboard shows the redesigned slim top bar plus both rails with real backing data, the lobby's roster panel is measurably content-sized (~221px, well under the fixed-third height a 1080px-tall viewport third would imply) rather than a rigid fixed split, and the playing state correctly drops the top bar row in favor of an unbroken video + right rail with participants/queue/QR-ticket. The one intentionally-absent element (connection pill) was verified against its own source as by-design (only renders during non-steady connection states). The error path surfaced the friendly `room-unavailable` screen with a working back-to-dashboard action for a valid-format-but-unknown code, exercised from a fresh, re-authenticated context as required. No jsErrors and no app-originated networkErrors fired across the entire walk (third-party YouTube/ad-network console noise was present but explicitly out of scope per task instructions). No blockers were hit.
