# ui-overhaul run — 2026-07-16

## Step 1 — Phase 1 enforcer review (sonnet): 0 critical, 0 major, 1 minor (features/index.md group-karaoke status fix — intentional, by feature-tracker). All 5 non-negotiables satisfied; tokens registered :root/.dark/@theme inline; typecheck clean.

```
Enforcer verdict: Clean. Phase 1 COMPLETE.
```

## Step 2 — Phase 1 — design system foundation (sonnet builder): design.md authored; app.css tokens rewritten (dark night-club default + light kept, pink→gold gradient, TV type scale, AA contrast tuning); Bricolage Grotesque + Inter self-hosted via fontsource (Google CDN removed); dark default theme; .brain design-system.md replaced. Committed f44c013.

```
typecheck exit 0; tests 401/401; build client+SSR PASS. Enforcer: 0 critical, 0 major, 1 intentional minor.
```

## Step 3 — Phase 2 — strip boilerplate (sonnet builder): file-upload feature + analytics chain deleted (14 files, no other consumers); home stubbed; dashboard slimmed; /admin→/admin/users redirect; Acme→Home Karaoke en+zh; feat-003/feat-004 cut. Kept /admin/users + kitchen-sink per review. Committed 4b1998e.

```
Purge greps clean (no acme/saas-starter/placeholder links). typecheck 0; tests 362/362; build PASS. Enforcer clean.
```

## Step 4 — Phase 3 — landing + auth + dashboard rebuild (sonnet builder): playful landing (hero, JoinRoomCard XXX-XXX code input → /join/<code>, how-it-works), AuthShell karaoke copy (StackBadge deleted), dashboard host hub w/ gradient CTA. room-code helpers +11 tests. Committed 21ed65c.

```
typecheck 0; tests 373/373; build PASS; SSR smoke: headline + testids + /login 200 verified live. Enforcer clean 0/0/0.
```

## Step 5 — Phase 4 — host TV reskin (sonnet builder): lobby/playing states, roster avatar chips, added-by chips on queue rows (attribution already single-writer in feat-007 protocol), one-shot lobby→playing confetti (ref-latched, reduced-motion gated), persistent corner QR, tv-* 10-foot scale + safe margins. Fixed tailwind-merge vs custom tv-* utilities conflict. Uncommitted — enforcer running.

```
typecheck 0; tests 373/373; build PASS. Live authed smoke: room created, lobby SSR (room-lobby/QR/code/roster testids) verified, bad-code friendly state, room closed.
```

## Step 6 — Phase 4 enforcer: 0 critical/major substance, 3 minors — all fixed inline (compact queue-row markup restored, text-white→text-primary-foreground, JoinPanel sm root = Card). Committed.

```
typecheck 0; tests 373/373 after fixes. Commit: phase 4 host TV reskin.
```

## Step 7 — Phase 5 — guest phone reskin + quick wins (sonnet builder): nickname/search/queue tab reskin, random-name spinner, tap-reorder (moveUp/moveToTop) reusing existing queue.reorder WS message (no protocol/DO change), position bar, queue-almost-full warning, host Controls tab gradient retheme

```
typecheck 0; tests 388/388 (15 new: party-names + room-state moveUp/moveToTop/ownQueueStanding); build PASS; SSR smoke: /join/FAKE-1 friendly not-found, /join/<real-code> shows join-nickname-randomize + Ready-to-join-the-party heading
```

## Step 8 — Phase 5 enforcer: clean, zero findings (single-writer reorder verified drag+tap identical path; gating matches canPerform; helper tests cover edges). Committed phase 5.

```
typecheck 0; tests 388/388.
```

## Step 9 — Phase 6a enforcer: 1 major (entrance animation never fired — ref seeded one render early) fixed inline + isNew latched in QueueRow against mid-flight cancel; 0 critical/minor. Committed.

```
typecheck 0; tests 401/401 after fix.
```

## Step 10 — Verifier regression fix: TV lobby start-party (no way to start party or see queue from a cold lobby)

```
Root cause: room-play-pause-button (HostControls) + QueueRail lived inside the grid gated by `hidden` while !hasCurrentItem, and hasCurrentItem only flips true AFTER playback.play is sent -> no affordance to send it from the TV in a cold lobby with songs queued (feature-verifier Finding #1 + #3, .brain/features/ui-overhaul/verifications/2026-07-16.md).

Fix in app/routes/room/\$code.tsx lobby block (~line 480-600):
1. When queue.length > 0, lobby now renders a compact queue summary (room-lobby-queue-summary: queue.count badge + first 3 titles w/ thumbnail + added-by InitialsAvatar chip, capped list so no scroll needed) plus a big gradient "Start the party" button (data-testid=room-lobby-start-party, reused t('player.start_party') i18n key already shipped in room.json from feat-007/youtube-player.tsx overlay -- no new i18n keys needed). Button onClick sends the EXACT same message HostControls' play button sends: send({ type: 'playback.play' }) -- no new wire protocol. applyClientMessage in app/lib/room-state.ts pops the queue head into currentItem on a null-currentItem playback.play, flipping hasCurrentItem true and swapping to the playing-state grid.
2. queue.length === 0 -> lobby unchanged (QR + prompt + roster), verified visually identical.
3. Overflow fix: lobby div gained min-h-0 (flex item was refusing to shrink below content, so overflow-y-auto never actually engaged -- content just grew past the viewport and got silently clipped by the root's overflow-hidden). RosterStrip wrapped in a max-h-32 overflow-y-auto cap. Restructured into a responsive layout: JoinPanel+roster form one column, queue-summary+button form a second column, side-by-side (lg:flex-row) once queue.length > 0 -- stacking everything in one column overflowed a 1920x1080 TV viewport (JoinPanel size='lg' alone is ~600px tall), clipping the start-party button below the fold on first pass.

Verify tails:
- bun run typecheck: PASS (cf-typegen + react-router typegen + tsc -b, no errors)
- bun run test: PASS, 401 tests / 31 files
- bun run build: PASS (built in ~8s, only pre-existing sourcemap warnings, no errors)

Live browser check (throwaway Playwright script via bun, headless, deleted after): admin@test.local host + guest context join via /join/:code, guest paste-URL-added https://www.youtube.com/watch?v=dQw4w9WgXcQ, host TV context (1920x1080) showed room-lobby-queue-summary + room-lobby-start-party visible, clicked it -> room-now-singing banner appeared (playing state). Measured document.documentElement.scrollHeight === window.innerHeight === 1080 (no overflow) and room-roster-chip bottom edge (1005px) within the tv-safe ~54px bottom margin (1026px boundary). Screenshot saved to .brain/features/ui-overhaul/screenshots/14-lobby-start-party.png and registered via brain shots add.
```
