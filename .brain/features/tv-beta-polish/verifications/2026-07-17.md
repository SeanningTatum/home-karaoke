# Verification: TV Beta Feedback Polish

- **Slug**: `tv-beta-polish` (feature memo: `.brain/features/tv-beta-polish/tv-beta-polish.md`)
- **Date**: 2026-07-17
- **Verified by**: feature-verifier (Playwright CLI — headless script via `bun`, chromium, `--disable-extensions`)
- **Base URL**: http://localhost:5173 _(dev server: started by agent, `bun run dev`)_
- **Role**: host = freshly signed-up account (`tv-verify-host-<ts>@test.local`, created via UI sign-up), guest = anonymous Better Auth session via nickname flow
- **Viewports**: TV context 1920×1080 (host), phone contexts 390×844 (guest + host-on-phone)
- **Branch**: `feat/tv-beta-polish`
- **Verdict**: ✅ **PASS** — every scoped beta-feedback item (lobby two-column/no-scroll, playing-state video-dominant layout, opaque "you're up" overlay with ~5s hold + animated exit, TV play/skip removed, 36px title clamp) verified live against the running app with zero jsErrors/networkErrors.

## Golden path

Host signs up → creates a room (TV, 1920×1080) → guest joins on phone (390×844) and adds 3 real, embeddable YouTube videos via the keyless paste-a-link fallback → host signs in on a **separate** phone context and drives playback from the Controls tab → TV enters the playing state → a skip triggers a genuine singer change (the "you're up" overlay) → a second skip lands on a 91-character real video title to exercise the 2-line clamp.

| # | Step | Expected | Observed | Screenshot | Result |
|---|------|----------|----------|------------|--------|
| 01 | Host signs up, creates room; TV lobby renders at 1920×1080 | Two-column layout (QR hero LEFT, roster board RIGHT), zero page scroll, playback hint pointing to phone, **no** play button anywhere on the TV | `document.scrollHeight`(1080) ≤ `innerHeight`(1080); QR panel's right edge ≤ roster panel's left edge; `room-lobby-playback-hint` visible ("Control playback from your phone — scan to open the Controls tab"); `room-play-pause-button` count = 0 | [`01-tv-lobby-empty.png`](../screenshots/01-tv-lobby-empty.png) | ✅ |
| 02 | Guest joins `/join/:code` on phone (390×844), anonymous nickname flow ("BetaGuest") | TV roster board shows the guest chip with an entrance-animation class; guest gets no Controls tab (host-only gating) | Chip found in `room-lobby-roster-panel` with class `animate-chip-in …`; guest's `join-tab-controls` count = 0 | [`02-phone-guest-joined.png`](../screenshots/02-phone-guest-joined.png), [`02-tv-roster-guest-joined.png`](../screenshots/02-tv-roster-guest-joined.png) | ✅ |
| 03 | Guest pastes a YouTube link (keyless paste-a-link fallback) — Rick Astley "Never Gonna Give You Up" | TV lobby queue preview updates live | `room-lobby-queue-summary` appears; `room-lobby-queue-count` reads "1 song" | [`03-phone-guest-added-song.png`](../screenshots/03-phone-guest-added-song.png), [`03-tv-lobby-queue-updated.png`](../screenshots/03-tv-lobby-queue-updated.png) | ✅ |
| — | Guest adds 2 more real videos (PSY "Gangnam Style"; a 91-char-titled karaoke medley) while still in the lobby, for later skip/clamp steps | Queue grows to 3 | Confirmed via later steps (Controls tab shows "3 songs") | (see step 04 screenshot) | ✅ |
| 04 | Host, on a **separate** phone context, signs in (`/login`) and opens `/join/:code` → Controls tab → presses play | Controls tab present (host-only); pressing play moves the TV into the playing state | `join-tab-controls` visible; `join-controls-tab` visible with 3 queued songs; `room-playing-rail` appears on the TV moments later | [`04-phone-host-controls-tab.png`](../screenshots/04-phone-host-controls-tab.png) | ✅ |
| 05 | TV playing state at 1920×1080 | Video dominant LEFT with title below (~36px, wraps, not the old 56px); RIGHT rail ~320px (participants top / queue middle / QR bottom); **no** play/skip buttons anywhere on the TV; zero page scroll | `room-playing-rail` width = 320px; `room-now-singing p.tv-title` computed font-size = 36px; `room-play-pause-button`/`room-skip-button` count = 0 on the TV; scrollHeight(1080) ≤ innerHeight(1080) | [`05-tv-playing-state.png`](../screenshots/05-tv-playing-state.png) | ✅ |
| 06 | Host phone: skip → a genuine singer change (Rick Astley → Gangnam Style) fires the "you're up" overlay | Fully opaque announcement overlay (no video visible through it), holds ~5s, then animates out | Overlay's computed `opacity` = `1`, `fixed inset-0` bounding box = exactly 1920×1080 (0.4s after visible, past the 0.3s fade+scale entrance); still visible at +2.4s; gone (count = 0) by ~+5.8s after the CSS exit animation. See note below on why this is exercised via skip rather than the very first play. | [`06a-tv-overlay-appears.png`](../screenshots/06a-tv-overlay-appears.png), [`06b-tv-overlay-holding.png`](../screenshots/06b-tv-overlay-holding.png), [`06c-tv-overlay-exiting.png`](../screenshots/06c-tv-overlay-exiting.png), [`06d-tv-overlay-gone.png`](../screenshots/06d-tv-overlay-gone.png) | ✅ |
| 07 | Host phone: skip again → lands on a real 91-character video title ("80s BEST FEMALE LOVE SONG MEDLEY (Karaoke) All This Time, Lost In Your Eyes, Eternal Flame") | Title wraps to max 2 lines with clamp, no layout break, page still doesn't scroll | `line-clamp: 2` computed; rendered height (82.8px) ≈ 2× line-height (41.4px), not the ~4+ lines an unclamped 91-char string would need; scrollHeight(1080) ≤ innerHeight(1080) | [`07-tv-long-title-clamped.png`](../screenshots/07-tv-long-title-clamped.png) | ✅ |

**Note on step 06 (overlay-trigger choice):** `app/routes/room/$code.tsx`'s `hasSeenFirstItemRef` latch intentionally suppresses `NowUpOverlay` on the very first lobby→playing transition — that moment is `CelebrationBurst`'s job instead (confirmed one-time visually during step 04/05; not separately screenshotted since it isn't in this feature's scope). `NowUpOverlay` fires on every **subsequent** singer change, so the golden path exercises it via a real skip — the same code path the app uses for every "you're up" announcement after the first song. This is documented, intentional behavior per the file's own doc comments, not a workaround.

## Error / edge path

Two edge cases were exercised (one dedicated, one folded naturally into the golden path per the instructions' own framing):

| # | Step | Expected failure/edge surface | Observed | Screenshot | Result |
|---|------|-------------------------------|----------|------------|--------|
| E1 | TV lobby with 0 guests (captured right after room creation, before the guest joins in step 02) | Friendly empty-roster state, not a blank panel | `room-roster-empty` visible: "Be the first to scan and join" | [`01-tv-lobby-empty.png`](../screenshots/01-tv-lobby-empty.png) | ✅ |
| 07 | Long-title (91 chars) song becomes the now-playing item | Clamped to 2 lines, no layout break (see golden-path row 07 above — this is simultaneously the requested "long-title error path") | See row 07 | [`07-tv-long-title-clamped.png`](../screenshots/07-tv-long-title-clamped.png) | ✅ |

Both ran in the SAME authenticated contexts as the golden path (not a fresh/unauthenticated context) — appropriate here since neither is an auth/permission failure surface (unlike a bad-login check), just UI edge states within the already-authenticated TV/host/guest flow.

## Console

Collected via `pageerror` + `console` (split: real JS/hydration errors vs. `Failed to load resource … status of <n>` / response ≥ 400) across all four browser contexts (TV host, guest phone, host phone, plus the pre-run warm-up pass).

- **jsErrors**: none.
- **networkErrors**: none.

No entries were suppressed or reclassified — the run was clean end-to-end once the dev-server cold-start artifact (below) was worked around.

## Findings for main thread

- **None app-side.** All `data-testid`s needed for this walk already existed (`room-lobby`, `room-lobby-roster-panel`, `room-roster-empty`, `room-roster-chip`, `room-lobby-queue-summary`, `room-lobby-queue-count`, `room-playing-rail`, `room-now-singing`, `room-now-up-overlay`, `join-tab-controls`, `join-controls-tab`, `room-play-pause-button`, `room-skip-button`, etc.) — no missing testid blocked this walk.
- **Environment-only observation (not a bug, no code touched):** a cold dev server needs a warm-up pass before scripted browser interaction — Vite's on-demand `optimizeDeps` discovery triggers a full client reload ("✨ optimized dependencies changed. reloading") the first time a not-yet-bundled dependency (e.g. `@radix-ui/react-alert-dialog`, `@dnd-kit/*`, `@radix-ui/react-tabs`) is imported by a route visited for the first time in that dev session. If a real user action (form fill + submit) races that reload, the in-flight submit can double as a bare HTML form GET or hit a stale React tree ("Invalid hook call" / `useMemo` on null). This is pure local-dev tooling behavior (Vite's dependency pre-bundling), not present in `bun run build`/production, and is unrelated to the `tv-beta-polish` feature — noted here only because it cost setup time and could bite the next agent running Playwright against a just-started `bun run dev`. Fix used: a throwaway warm-up pass visiting sign-up → dashboard → room → join once before the real run.
- **Hydration-race guard added to the verifier's own script (not app code):** `.fill()` immediately after `page.goto()` can commit into a `<input>` a beat before React finishes hydrating and resets the controlled value to its pre-hydration default, silently clobbering the typed text. Worked around locally in the throwaway script (`reliableFill` re-checks the committed value after a short delay) — flagging in case this is useful context for any future in-repo Playwright tooling, but this is a test-script concern, not an app defect.

## Verdict rationale

All seven scoped beta-feedback items from `tv-beta-polish.md` were exercised against the live app and passed: the lobby is a genuine two-column, zero-scroll layout with a phone-hint and no TV play button; a guest join animates into the roster and a pasted link updates the live queue; the host's phone Controls tab is the only playback surface and pressing play flips the TV into the playing state; the playing state puts the video dominant on the left (title clamped to `tv-title`/36px, not the old 56px `tv-headline`) with a measured ~320px right rail and zero on-screen TV transport controls; the "you're up" overlay is measured fully opaque (`opacity: 1`, full-viewport bounding box) and holds for the documented ~5s window before its CSS exit completes; and a real 91-character video title clamps to exactly 2 lines without breaking the layout or introducing page scroll. No `jsErrors` or `networkErrors` were observed on any golden-path or edge-path step. Verdict: **PASS**.

## Addendum — post-fix re-capture

- **Date**: 2026-07-17 (same-day re-capture, after the original walk above)
- **Commit**: `fa03819` — "fix(tv): lobby alignment + readable queue rows per beta screenshots" (landed on `feat/tv-beta-polish` after the original walk in this doc)
- **Reason**: screenshots `01`/`02`/`03`/`05`/`07` were captured from the PRE-fix layout; `fa03819` changed the lobby room-code/heading/playback-hint placement, moved the lobby queue preview into the roster's right column, and widened the TV playing-rail queue rows (text-base 2-line clamp, visible added-by nickname, 56px thumbnails). This addendum re-verifies the same golden-path steps against the POST-fix app and refreshes the 5 affected screenshots (same step names — the brain's `shots add` overwrote the stale images in place; steps `02`/`04`/`06a-d`/`13` were untouched since their underlying UI didn't change in `fa03819`).
- **Method**: same throwaway Playwright CLI script pattern as the original walk (`bun run tmp/verify-tv-beta-polish.ts`, chromium `--disable-extensions`, dev server started via `bun run dev` and stopped after), reusing this doc's recipe — sign-in, room creation, guest join, paste-add, host-phone Controls-tab play/skip — plus the two documented environment workarounds (cold-dev-server warm-up pass; `.fill()` hydration-race retry). Two additional real-app quirks surfaced and were worked around in the script only (not app code): (a) `SearchTab`'s paste form unmounts/resets every time a successful add flips the guest to the Queue tab, so each subsequent paste re-selects the Search tab and reopens paste mode; (b) the host, despite already having an authenticated + host session, still passes through the same nickname-entry gate as a guest on `/join/:code` before the Controls tab renders (`joined` state starts `null` regardless of `isHost`/`hasSession`).
- **What was re-verified against the live post-fix app**:
  - **01 — TV lobby, fresh room**: room code renders as a single line (`codeLines` computed = 1, no mid-code wrap), centered in the QR card; the playback hint (`room-lobby-playback-hint`) is small fine-print *inside* the QR card, not an orphaned banner; "Party lobby" (`room-lobby-heading`) is a page-level header top-left, above both columns; the empty roster state shows the bordered icon circle. Zero page scroll confirmed (`scrollHeight` 1080 ≤ `innerHeight` 1080).
  - **02 — guest joined**: guest chip ("BetaGuest") renders on the right roster board, matching the original walk.
  - **03 — queue updated in lobby**: after the guest pastes a song, the queue preview (`room-lobby-queue`) now sits in the RIGHT column, directly under the roster panel (`room-lobby-roster-panel`) — same left edge as the roster, and clearly right of the QR panel. Measured geometrically: queue's left edge equals the roster's left edge (674px, Δ<5px) and both sit right of the QR panel's right edge. Confirmed visually in the screenshot too.
  - **05 — TV playing state, queue-row readability (the one live check the original fix never got)**: **PASS.** First queue row measured — title text `"PSY - GANGNAM STYLE(강남스타일) M/V"` fully readable (not truncated to "8…"), computed `font-size: 16px` (`text-base`) with `line-clamp: 2`; added-by nickname renders as the full `"BetaGuest"` (not truncated to "B."); thumbnail bounding box 56×56px. Visually confirmed in the screenshot: all three queue rows (PSY, Tom Jones Medley, 80s medley) show real, legible 2-line titles with full nicknames and real video thumbnails. Zero page scroll held (1080/1080).
  - **07 — long title clamp**: re-added the actual 91-character real YouTube title used in the original walk ("80s BEST FEMALE LOVE SONG MEDLEY (Karaoke) All This Time, Lost In Your Eyes, Eternal Flame", video id `O2BSsYTSw5M`) and skipped to it; clamps to exactly 2 lines (`line-clamp: 2`, rendered height 82.8px ≈ 2× line-height 41.4px), no layout break, zero scroll. Screenshot captured only after the "you're up" overlay from the preceding skip fully cleared (waited for `room-now-up-overlay` to detach), so the image shows the actual clamped title rather than the announcement card.
- **jsErrors**: none across all contexts (TV host, guest phone, host phone).
- **networkErrors**: none.
- **Regressions**: none found. All 5 re-captured states match the intended fix from `fa03819` with no new console errors or layout breaks.
- **Verdict**: ✅ **PASS** — the `fa03819` layout fix is confirmed live: lobby alignment items (room code, playback hint, heading, queue placement) render as intended, and the previously-unverified TV playing-rail queue-row readability fix is now confirmed live (readable titles, visible nicknames, 56px thumbnails) with zero scroll and zero console errors.
