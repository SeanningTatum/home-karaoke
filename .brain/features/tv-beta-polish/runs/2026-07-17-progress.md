# tv-beta-polish run — 2026-07-17

## Step 1 — Task kickoff: plan plans/tv-beta-feedback.html reviewed round 1 (3 decisions), feat-009 in-progress, branch feat/tv-beta-polish created

```
init.sh --baseline: typecheck PASS, test PASS, harness-check 11/11 PASS. Baseline green on main @ fd70c44.
```

## Step 2 — Phase 1 COMPLETE (sonnet builder): lobby two-column grid lg:grid-cols-[minmax(420px,560px)_1fr], QR+hint left, roster board right (auto-fill chip grid, internal scroll, size-20 avatars), max-w-[1700px], overflow-hidden at lg+ so page never scrolls; i18n lobby.playback_hint en+zh

```
bun run typecheck: exit 0. bun run test: Test Files 31 passed (31), Tests 404 passed (404), Duration 3.58s
```

## Step 3 — Phase 2 COMPLETE (opus builder): playing grid → left title(36px line-clamp-2)+fill video, right rail fixed 320px (compact roster / queue w/ 22px tv-title-sm heading / QR pinned bottom); HostControls removed from TV (kept — phone ControlsTab imports it); lobby start-party button removed, playback_hint copy now points to phone

```
bun run typecheck: exit 0. bun run test: Test Files 31 passed (31), Tests 404 passed (404), Duration 3.36s
```

## Step 4 — Phase 3 COMPLETE (sonnet builder): NowUpOverlay backdrop fully opaque (alpha modifiers dropped), dismiss 2500→5000ms, exit anim animate-now-up-out 0.45s w/ animationend + 600ms fallback unmount guarantee; pure transition helper app/lib/now-up-overlay-state.ts +8 tests; reduced-motion now shows static card (matches plan edge-case row)

```
bun run typecheck: exit 0. bun run test: Test Files 32 passed (32), Tests 412 passed (412)
```

## Step 5 — Phase 4 COMPLETE (sonnet builder): party-sounds → JingleSpec data model; JOIN 3-note sine arpeggio C5-E5-G5 ~295ms, ADD triangle coin A5-E6-A6 ~270ms, new ~870ms square+saw fanfare via playFanfare() fired inside existing nowUpSinger effect (shares hasSeenFirstItemRef gate — cannot fire on first-connect); gain 0.12→0.24 peak w/ 0.3 ceiling; same mute toggle gates all

```
bun run typecheck: exit 0. bun run test: Test Files 32 passed (32), Tests 424 passed (424) — party-sounds suite 28 tests
```

## Step 6 — Non-negotiables enforcer review of full branch diff (eb5aa63..a84162d)

```
Clean — all 5 non-negotiables satisfied. WebAudio exemption pattern preserved; now-up-overlay-state.ts has co-located tests; HostControls not orphaned (phone ControlsTab imports it); no process.env/Zod/tagged-error surface touched.
```

## Step 7 — feature-verifier browser walk (1920x1080 TV + 390x844 phone contexts)

```
PASS 7/7 golden steps + zero-guest empty state + 91-char title clamp edge. Overlay measured opacity:1 full-viewport, holds ~5s, gone by +5.8s. Right rail measured 320px. No TV play/skip anywhere. jsErrors: none. networkErrors: none. Doc: .brain/features/tv-beta-polish/verifications/2026-07-17.md, 12 screenshots registered.
```

## Step 8 — User beta-screenshot feedback after ship: lobby text alignment off (room code wraps mid-code, orphaned uppercase hint, floating PARTY LOBBY heading), queue preview should join roster on right side; playing-state queue rows unreadable in 320px rail (title truncates to '8…', added-by to 'B.'), container polish needed. Opus fixer agent dispatched with both screenshots

```
Screenshots: image-cache 2.png (lobby), 3.png (playing). Feature stays shipped; fix pass on same branch pre-PR.
```

## Step 9 — Beta-screenshot fix COMPLETE (opus fixer): tv-code 80px token (code never wraps, centered), hint moved inside QR card as calm fine-print, PARTY LOBBY → page-level header, queue preview moved to right column w/ roster, roster empty state redesigned, TV queue rows compacted (title text-base line-clamp-2, added-by full name text-sm, 56px thumbs)

```
typecheck exit 0; tests 424/424; lobby visual self-check PASS at 1920x1080 (scrollHeight 1080 == innerHeight), screenshot registered as 13-lobby-alignment-fix. Playing-state rows verified by code review only — live playing walk impractical in fixer scope.
```

## Step 10 — Post-fix screenshot refresh (feature-verifier): 01/02/03/05/07 re-captured at fa03819, verification doc addendum appended

```
Queue-row readability PASS live: title 16px line-clamp-2 (was '8…'), added-by full 'BetaGuest' (was 'B.'), thumb 56x56. Lobby fix confirmed in all recaptures. Zero page scroll 1080/1080. jsErrors none, networkErrors none, regressions none.
```

## Step 11 — User pin-annotated 9 fixes via brain shots notes (lobby: participants 1/3 + always-visible queue right, equal-height QR card, navbar title, drop TV mute button; phone Controls: start/end-party section, remove redundant buttons; playing: rail slimmer + full height, stacked queue cards). Opus fixer dispatched w/ screenshots + pin coords. PR #5 conflicts w/ main resolved earlier (99f177f), CI green, merge deferred until these land

```
annotations.json: 9 open pins across 01/03/04/05 screenshots
```

## Step 12 — All 9 pin annotations fixed (opus): lobby right col = participants 1/3 + always-visible queue w/ empty state, QR card full column height, PARTY LOBBY navbar title, TV mute button removed (persisted default honored); phone Controls PARTY section (Start via playback.play, End via room.close + confirm dialog), redundant tap-reorder arrows removed (drag kept); playing rail 320→280px full viewport height, queue rows now vertical stacked cards. 4 screenshots re-captured, all annotations superseded

```
typecheck 0; tests 429/429; playing scrollHeight 1080==1080; rail box {x:1560,y:48,w:280,h:984}. Coordinator eyeballed all 4 shots vs pins — match.
```
